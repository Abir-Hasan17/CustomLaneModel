# Helper outlines to integrate with CULane official eval. This is a placeholder that explains steps.
# 1) Run infer.py across the validation list and save lane polylines to text files per image
# 2) Convert to the evaluator's expected format (see SCNN tools/lane_evaluation)
# 3) Call the evaluator and parse IoU/F1 per category

